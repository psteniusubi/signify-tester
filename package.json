{
  "name": "signify-tester",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "publish-github": "tsc && vite build --base=/signify-tester/ --outDir=./docs/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.1.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "signify-ts": "file:./signify-ts"
  }
}